import time
import board
import neopixel
import os
import RPi.GPIO as GPIO
GPIO.setmode(GPIO.BCM)
from luma.core.interface.serial import i2c
from luma.core.render import canvas
from luma.oled.device import ssd1306, ssd1325, ssd1331, sh1106

serial = i2c(port=1, address=0x3C)
device = ssd1306(serial, rotate=0)

pixels1 = neopixel.NeoPixel(board.D18, 30, brightness=1)
x=0
y=0
pixels1.fill((0,0,0))

GPIO.setwarnings(False)
GPIO.setup(4, GPIO.IN) #button 7
GPIO.setup(23, GPIO.IN)  #button 16
GPIO.setup(24, GPIO.IN)  #switch 18
GPIO.setup(25, GPIO.IN)  #switch 22
GPIO.setup(8, GPIO.IN)  #switch 24
GPIO.setup(7, GPIO.IN)  #switch 26
GPIO.setup(12, GPIO.IN)  #switch 32
GPIO.setup(16, GPIO.IN)  #switch 36
GPIO.setup(20, GPIO.IN)  #switch 38
GPIO.setup(21, GPIO.IN)  #switch 40


GPIO.setup(26, GPIO.OUT)
GPIO.setup(19, GPIO.OUT)


print(" Button + GPIO ")

while True:
    if ( GPIO.input(4) == False ):
        print("Button pressed")
        os.system('date')
        with canvas(device) as draw:
            draw.text((10, 40), "Hello World", fill="white")
            draw.text((10, 50), "from Carson Soers!", fill="white")
            GPIO.output(26, True)
        time.sleep(5)
        with canvas(device) as draw:
            draw.text((10, 40), "Hello World", fill="black")
            draw.text((10, 50), "from Edward!", fill="black")
            GPIO.output(26, False)
    if ( GPIO.input(23) == False ):
        print("Button pressed")
        os.system('date')
        GPIO.output(19, True)
        time.sleep(1)
        GPIO.output(19, False)
    if ( GPIO.input(24) == False ):
        print("Button pressed")
        with canvas(device) as draw:
            draw.text((10, 40), "Blue", fill="white")
        while x<20:   
            pixels1[x]=(0,0,10)
            x=x+1
            time.sleep(0.1)
        while y<20:   
            pixels1[y]=(0,0,0)
            y=y+1
            time.sleep(0.1)
        time.sleep(1)
        with canvas(device) as draw:
            draw.text((10, 40), "", fill="black")
        x=0
        y=0
    if ( GPIO.input(25) == False ):
        print("Button pressed")
        with canvas(device) as draw:
            draw.text((10, 40), "Red", fill="white")
        while x<20:   
            pixels1[x]=(10,0,0)
            x=x+1
            time.sleep(0.1)
        while y<20:   
            pixels1[y]=(0,0,0)
            y=y+1
            time.sleep(0.1)
        time.sleep(1)
        with canvas(device) as draw:
            draw.text((10, 40), "", fill="black")
        x=0
        y=0
        
    if ( GPIO.input(8) == False ):
        print("Button pressed")
        with canvas(device) as draw:
            draw.text((10, 40), "Green", fill="white")
        while x<20:   
            pixels1[x]=(0,10,0)
            x=x+1
            time.sleep(0.1)
        while y<20:   
            pixels1[y]=(0,0,0)
            y=y+1
            time.sleep(0.1)
        time.sleep(1)
        with canvas(device) as draw:
            draw.text((10, 40), "", fill="black")
        x=0
        y=0
    if ( GPIO.input(7) == False ):
        print("Button pressed")
        with canvas(device) as draw:
            draw.text((10, 40), "Yellow", fill="white")
        while x<20:   
            pixels1[x]=(30,30,0)
            x=x+1
            time.sleep(0.1)
        while y<20:   
            pixels1[y]=(0,0,0)
            y=y+1
            time.sleep(0.1)
        time.sleep(1)
        with canvas(device) as draw:
            draw.text((10, 40), "", fill="black")
        x=0
        y=0

#     if ( GPIO.input(12) == False ):
#         print("Button pressed")
#         with canvas(device) as draw:
#             draw.text((10, 40), "Purple", fill="white")
#         while x<20:   
#             pixels1[x]=(30,0,30)
#             x=x+1
#             time.sleep(0.1)
#         while y<20:   
#             pixels1[y]=(0,0,0)
#             y=y+1
#             time.sleep(0.1)
#         time.sleep(1)
#         with canvas(device) as draw:
#             draw.text((10, 40), "", fill="black")
#         x=0
#         y=0

    if ( GPIO.input(16) == False ):
        print("Button pressed")
        with canvas(device) as draw:
            draw.text((10, 40), "Cyan", fill="white")
        while x<20:   
            pixels1[x]=(0,30,30)
            x=x+1
            time.sleep(0.1)
        while y<20:   
            pixels1[y]=(0,0,0)
            y=y+1
            time.sleep(0.1)
        time.sleep(1)
        with canvas(device) as draw:
            draw.text((10, 40), "", fill="black")
        x=0
        y=0

    if ( GPIO.input(20) == False ):
        print("Button pressed")
        with canvas(device) as draw:
            draw.text((10, 40), "Orange", fill="white")
        while x<20:   
            pixels1[x]=(30,10,0)
            x=x+1
            time.sleep(0.1)
        while y<20:   
            pixels1[y]=(0,0,0)
            y=y+1
            time.sleep(0.1)
        time.sleep(1)
        with canvas(device) as draw:
            draw.text((10, 40), "", fill="black")
        x=0
        y=0

    if ( GPIO.input(21) == False ):
        print("Button pressed")
        with canvas(device) as draw:
            draw.text((10, 40), "Pink", fill="white")
        while x<20:   
            pixels1[x]=(30,5,10)
            x=x+1
            time.sleep(0.1)
        while y<20:   
            pixels1[y]=(0,0,0)
            y=y+1
            time.sleep(0.1)
        time.sleep(1)
        with canvas(device) as draw:
            draw.text((10, 40), "", fill="black")
        x=0
        y=0

    else:
        print ("waiting for you to press a button")
time.sleep(1)








# pixels1.fill =((0,0,200))

# while x<20:
#     
#     pixels1[x]=(255,0,0)
#     pixels1[x-5]=(255,0,100)
#     pixels1[x-10]=(0,0,255)
#     x=x+1
#     time.sleep(0.1)
#     
# pixels1.fill((0,0,0))
